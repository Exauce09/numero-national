/** Placeholder — GET /api/v1/me/access-log (data tracker) */
export default function AccessLogPage() {
  return (
    <section>
      <h1>Journal d’accès</h1>
      <p>Qui a consulté vos données (filtre audit / resource citoyen).</p>
      <div className="placeholder">À brancher sur <code>GET /api/v1/me/access-log</code></div>
    </section>
  );
}
