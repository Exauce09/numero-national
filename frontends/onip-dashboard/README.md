# ONIP Dashboard (Phase 7)

Minimal React + Vite + TypeScript console for national operators.

```bash
cd frontends/onip-dashboard
npm install
npm run dev
```

Proxies `/api` → `http://localhost:8000`. Primary endpoint:
`GET /api/v1/onip/dashboard`.
